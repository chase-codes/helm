// Empty-state copy shown in the Sermon tab (search rail + center stage) when no
// bible translation is installed. Shared so both surfaces read identically.
export const INSTALL_HINT = '[ Install a Bible in Settings ]';
